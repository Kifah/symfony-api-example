default:
    suites:
        api:
            contexts:
                - ApiContext:
                    client: '@guzzle_client'
            paths: [ %paths.base%/features/api ]

    extensions:
        Behat\Symfony2Extension: ~
